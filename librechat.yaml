mcpServers:
  gdrive:
    type: stdio
    command: npx
    args:
      - "-y"
      - "@modelcontextprotocol/server-gdrive"
    requiresOAuth: true
    url: "${DOMAIN_CLIENT}"  # Použije proměnnou z Railway, neřešíš ruční přepisování
    env:
      GDRIVE_OAUTH_JSON: "${GDRIVE_OAUTH_JSON}"
