version: 1.3.11
cache: true

# 1. SEKCE: ROZHRANÍ (ZAPÍNÁ GRAFICKÉ PRVKY)
interface:
  endpointsMenu: true
  modelSelect: true
  parameters: true      
  sidePanel: true
  presets: true
  bookmarks: true
  multiConvo: true
  artifacts: true      
  agents:
    use: true
    create: true       

# 2. SEKCE: ROZŠÍŘENÉ MCP SERVERY (PŘESUNUTO NAHORU PRO JISTOTU NAČTENÍ)
mcpServers:
  google-drive:
    type: streamable-http
    url: "https://composio.dev"
    headers:
      x-api-key: "${COMPOSIO_API_KEY}"
    timeout: 60000

  web-fetch:
    type: streamable-http
    url: "https://smithery.ai"
    timeout: 60000

# 3. SEKCE: WEBOVÉ VYHLEDÁVÁNÍ TAVILY
webSearch:
  searchProvider: "tavily"
  tavilyApiKey: "${TAVILY_API_KEY}"

# 4. SEKCE: STRATEGIE PRO SOUBORY (RAG)
fileConfig:
  endpoints:
    assistants:
      fileLimit: 10
      fileSizeLimit: 104857600 
    agents:
      fileLimit: 20
      fileSizeLimit: 104857600
  serverFileSizeLimit: 104857600

# 5. SEKCE: SEZNAM AI MODELŮ
endpoints:
  anthropic:
    apiKey: "${ANTHROPIC_API_KEY}"
    fetch: true                     
  google:
    apiKey: "${GOOGLE_API_KEY}"
    models:
      - "gemini-2.5-flash"
      - "gemini-2.5-pro"
    fetch: true
  agents:
    disableBuilder: false
  custom:
    - name: "DeepSeek Chat"
      type: "openai"
      apiKey: "${DEEPSEEK_API_KEY}"
      baseURL: "https://deepseek.com"  
      models:
        default: ["deepseek-chat"]
        fetch: false
      titleConvo: true
      titleModel: "deepseek-chat"
      modelDisplayLabel: "DeepSeek Chat"
    - name: "DeepSeek Reasoner"
      type: "openai"
      apiKey: "${DEEPSEEK_API_KEY}"
      baseURL: "https://deepseek.com"  
      models:
        default: ["deepseek-reasoner"]
        fetch: false
      titleConvo: true
      titleModel: "deepseek-reasoner"
      modelDisplayLabel: "DeepSeek Reasoner"
