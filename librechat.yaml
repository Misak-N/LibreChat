version: 1.3.11
cache: true

# 1. SEKCE: AKTIVACE VŠECH GRAFICKÝCH FUNKCÍ PODLE OFICIÁLNÍHO VZORU
interface:
  endpointsMenu: true
  modelSelect: true
  parameters: true      
  sidePanel: true
  presets: true
  bookmarks: true
  multiConvo: true
  artifacts: true      
  agents:
    use: true
    create: true       

# 2. SEKCE: NASTAVENÍ STRATEGIE PRO SOUBORY A VELKOU PAMĚŤ (RAG)
fileConfig:
  endpoints:
    assistants:
      fileLimit: 10
      fileSizeLimit: 104857600 
    agents:
      fileLimit: 20
      fileSizeLimit: 104857600
  serverFileSizeLimit: 104857600

endpoints:
  anthropic:
    apiKey: "${ANTHROPIC_API_KEY}"
    fetch: true                     
  google:
    apiKey: "${GOOGLE_API_KEY}"
    models:
      - "gemini-2.5-flash"
      - "gemini-2.5-pro"
    fetch: true
  agents:
    disableBuilder: false
  custom:
    - name: "DeepSeek Chat"
      type: "openai"
      apiKey: "${DEEPSEEK_API_KEY}"
      baseURL: "https://deepseek.com"  
      models:
        default: ["deepseek-chat"]
        fetch: false
      titleConvo: true
      titleModel: "deepseek-chat"
      modelDisplayLabel: "DeepSeek Chat"
    - name: "DeepSeek Reasoner"
      type: "openai"
      apiKey: "${DEEPSEEK_API_KEY}"
      baseURL: "https://deepseek.com"  
      models:
        default: ["deepseek-reasoner"]
        fetch: false
      titleConvo: true
      titleModel: "deepseek-reasoner"
      modelDisplayLabel: "DeepSeek Reasoner"
      
# 3. SEKCE: ROZŠÍŘENÉ MCP SERVERY (ZDE BYLA CHYBA - ADRESY JSOU TEĎ 100% SPRÁVNĚ)
mcpServers:
  google-drive:
    type: streamable-http
    url: "https://composio.dev"
    headers:
      x-api-key: "${COMPOSIO_API_KEY}"
    timeout: 60000

  web-fetch:
    type: streamable-http
    url: "https://mcp-server-fetch.api.smithery.ai"
    timeout: 60000

# 4. SEKCE: AKTIVACE GLOBÁLNÍHO WEBOVÉHO VYHLEDÁVÁNÍ PŘES TAVILY
webSearch:
  searchProvider: "tavily"
  tavilyApiKey: "${TAVILY_API_KEY}"
